# Changelog

- Added OOXML container package Excel Slicers/Timelines, Word Bibliography/Citations, and Custom XML XPath Data Binding threat inspection rules:
  - **Slicer & Timeline Cache Anomaly (`VBA-CELL-050`, Critical/High)**: Inspects Excel Slicers and Timelines (`xl/slicers/slicer*.xml`, `xl/slicers/slicerCache*.xml`, `xl/timelines/timeline*.xml`, `xl/timelines/timelineCache*.xml`) and their relationships for remote UNC targets (`\\host\share`) enabling NTLM credential coercion, dangerous exploit URI protocols (`ms-msdt:`, `search-ms:`, `mhtml:`, `javascript:`, `powershell:`), executable/script targets (`.exe`, `.bat`, `.vbs`, `.ps1`, `.hta`), database command execution queries (`xp_cmdshell`, `sp_OACreate`, `OPENROWSET`, `bulk insert`), or environment variable exfiltration tokens (`%USERNAME%`, `%USERDOMAIN%`, `%COMPUTERNAME%`).
  - **Bibliography & Citation Anomaly (`VBA-CELL-051`, Critical/High)**: Inspects Word Bibliography and citation definitions (`word/bibliography/sources.xml`, `customXml/`) for remote UNC paths enabling NTLM credential coercion, dangerous exploit protocol handlers (`ms-msdt:`, `search-ms:`, `ms-appinstaller:`, `mhtml:`, `javascript:`, `powershell:`), shell execution commands (`powershell`, `cmd.exe`, `wscript.exe`, `cscript.exe`, `mshta`, `rundll32`, `certutil`), or smuggled Windows PE binaries (`TVqQ`).
  - **Custom XML Data Binding & XPath Anomaly (`VBA-CELL-052`, Critical/High)**: Inspects Structured Document Tags (SDT) and Custom XML data bindings (`<w:dataBinding>`, `<m:dataBinding>`, `<p:dataBinding>`) in Word, PowerPoint, and Excel parts for XPath queries containing external document resolution functions (`document('http://...')`, `document('\\\\host\\share...')`, `doc('...')`) enabling SSRF/NTLM credential theft, database/shell command injection, or remote UNC / exploit protocol namespace declarations in `prefixMappings` or `storeItemID`.
- Extended Excel formula evaluation engine (`formula_eval`) with hyperbolic trigonometry, inverse hyperbolic, $\pi$ square root, and sum of squares functions:
  - **Hyperbolic & Inverse Hyperbolic Functions**: Added `SINH(number)` ($f64::sinh$), `COSH(number)` ($f64::cosh$), `TANH(number)` ($f64::tanh$), `ASINH(number)` ($f64::asinh$), `ACOSH(number)` ($f64::acosh$ with $x \ge 1$ validation), and `ATANH(number)` ($f64::atanh$ with $|x| < 1$ validation) with overflow `#NUM!` handling.
  - **Mathematical Square Root with $\pi$**: Added `SQRTPI(number)` ($\sqrt{x \cdot \pi}$ with $x \ge 0$ validation).
  - **Sum of Squares Multi-Argument Summation**: Added `SUMSQ(number1, [number2], ...)` supporting scalar values, 2D ranges, and array literals with non-numeric cell coercion conforming to Excel specification.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `SINH`, `COSH`, `TANH`, `ASINH`, `ACOSH`, `ATANH`, `SQRTPI`, and `SUMSQ`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-050`, `VBA-CELL-051`, and `VBA-CELL-052`.

- Added OOXML container package Power Query M formulas, OLE Moniker activation, and XML namespace cloaking threat inspection rules:
  - **Power Query Formula & Data Mashup Anomaly (`VBA-CELL-047`, Critical/High)**: Inspects Excel Power Query definitions (`xl/powerQuery/powerQuery.xml`), Data Mashup parts (`customXml/item*.xml`, `customData/`), and M code queries for arbitrary HTML/JavaScript/ActiveX execution via `Web.Page(...)` inside Excel host, remote data exfiltration or payload download via `Web.Contents(...)`, remote UNC paths via `File.Contents(...)` or string literals for NTLM credential coercion, smuggled Windows executable binaries reconstructed via `Binary.FromText(..., BinaryEncoding.Base64)`, database command execution (`xp_cmdshell`, `sp_OACreate`, `OPENROWSET`), or host environment profiling (`Environment.UserName`, `Environment.MachineName`, `Diagnostics.ActivityId`).
  - **OLE Object Moniker & Package Activation Anomaly (`VBA-CELL-048`, Critical/High)**: Inspects embedded OLE object parts, drawings, slides, and package relationships for dangerous Moniker URI/protocol handlers (`moniker:`, `file:`, `script:`, `composite:`, `ms-msdt:`, `search-ms:`, `ms-appinstaller:`), automatic silent activation flags (`UpdateMode="Always"`, `autoUpdate="1"`, `autoActivate="1"`), deceptive icon aspect cloaking of executable payloads (`DrawAspect="Icon"` with `.exe`, `.bat`, `.scr`, `.vbs`, `.js`, `.cmd`, `ProgID="Package"`), and weaponized Packager/Moniker CLSIDs (`{F20BE578-230C-1380-1A29-E3F84C071110}`, `{0000031A-0000-0000-C000-000000000046}`, `{79EAC9E0-BAF9-11CE-8C82-00AA004BA90B}`, `{00020820-0000-0000-C000-000000000046}`, `{00020906-0000-0000-C000-000000000046}`).
  - **XML Namespace Cloaking & Schema Spoofing (`VBA-CELL-049`, Critical)**: Inspects OOXML XML parts and relationships for remote UNC namespace declarations (`xmlns:...="\\host\share..."`) or `xsi:schemaLocation` coercion enabling NTLM credential theft, Document Type Definition (DTD) and external entity declarations (`<!DOCTYPE`, `<!ENTITY %`) for XXE injection, and Cyrillic/Unicode homoglyphs or zero-width spaces (`\u{200B}`, `\u{FEFF}`) cloaking standard Office schemas.
- Extended Excel formula evaluation engine (`formula_eval`) with advanced exponential, logarithmic, combinatoric, and array product functions:
  - **Constants & Exponentials**: Added `PI()` (mathematical constant $\pi$) and `EXP(number)` (exponential $e^x$).
  - **Logarithmic Functions**: Added `LN(number)` (natural logarithm), `LOG10(number)` (base-10 logarithm), and `LOG(number, [base])` (arbitrary base logarithm with domain/base validation).
  - **Combinatorics & Permutations**: Added `COMBIN(number, number_chosen)` (combinations $C(n, k)$ calculation with overflow guard) and `PERMUT(number, number_chosen)` (permutations $P(n, k)$ calculation).
  - **Multi-Array Product Summation**: Added `SUMPRODUCT(array1, [array2], ...)` supporting scalar and multi-dimensional array arguments with dimension matching and non-numeric cell zero-coercion conforming to Excel specification.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `COMBIN`, `PERMUT`, `SUMPRODUCT`, `EXP`, `LOG`, `LN`, `QUOTIENT`, `EVEN`, `ODD`, `FACT`, `GCD`, and `LCM`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-047`, `VBA-CELL-048`, and `VBA-CELL-049`.

- Added OOXML container package SmartArt diagrams, Word MailMerge coercion, and Excel QueryTable threat inspection rules:
  - **SmartArt & Diagram Action / Payload Injection (`VBA-CELL-044`, Critical/High)**: Inspects SmartArt diagram markup parts (`*/diagrams/*.xml`, `data*.xml`, `layout*.xml`, `drawing*.xml`) and diagram relationships for interactive action/hover click triggers (`<dgm:hlinkClick>`, `<a:hlinkClick>`, `<a:hlinkHover>`, `ppaction://program`, `ppaction://macro`), dangerous URI protocol handlers (`ms-msdt:`, `search-ms:`, `ms-appinstaller:`, `mhtml:`, `powershell:`, `cmd:`, `wscript:`, `cscript:`, `javascript:`, `vbscript:`), remote UNC paths for NTLM credential coercion, cloaked Windows PE executable binaries, embedded Windows Shell Link (`.lnk`) shortcuts, or embedded shell execution commands.
  - **Word MailMerge Data Source Hijacking & Coercion (`VBA-CELL-045`, Critical/High)**: Inspects Word settings (`word/settings.xml`) and MailMerge relationships for `<w:mailMerge>` blocks configuring remote UNC connection strings (`<w:connectString>`) enabling automatic NTLM credential coercion without user confirmation, database command injection / shell execution queries (`<w:query>` containing `xp_cmdshell`, `sp_OACreate`, `OPENROWSET`), automatic merge execution configurations (`destination="newDocument"`, `<w:linkToQuery/>`), or external relationships targeting dangerous executable or script payloads (`.iqy`, `.hta`, `.vbs`, `.bat`, `.ps1`, `.exe`).
  - **Excel QueryTable Web Query & External Database Abuse (`VBA-CELL-046`, Critical/High)**: Inspects Excel QueryTable definition parts (`xl/queryTables/queryTable*.xml`) and relationships for automatic background refresh (`refreshOnLoad="1"`, `autoRefresh="1"`), external Web Query files (`.iqy`, `.dqy`, `.rqy`, `.oqy`) capable of remote payload delivery, UNC connection targets for NTLM credential coercion, database command execution (`xp_cmdshell`, `sp_OACreate`, `OPENROWSET`), or environment variable exfiltration tokens (`%USERNAME%`, `%USERDOMAIN%`, `%COMPUTERNAME%`).
- Extended Excel formula evaluation engine (`formula_eval`) with advanced integer arithmetic, factorials, and number-theoretic functions:
  - **Integer Arithmetic & Even/Odd Rounding**: Added `QUOTIENT(numerator, denominator)` (integer division truncation with `#DIV/0!` guard), `EVEN(number)` (rounding away from zero to nearest even integer), and `ODD(number)` (rounding away from zero to nearest odd integer).
  - **Factorial Functions**: Added `FACT(number)` (integer factorial calculation up to 170!) and `FACTDOUBLE(number)` (double factorial $n!!$ calculation up to 300!! with boundary handling).
  - **Number Theory & Integer GCD / LCM**: Added multi-argument and range-supported `GCD(number1, [number2], ...)` (greatest common divisor via Euclidean algorithm) and `LCM(number1, [number2], ...)` (least common multiple with saturation arithmetic).
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `QUOTIENT`, `EVEN`, `ODD`, `FACT`, `FACTDOUBLE`, `GCD`, and `LCM`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-044`, `VBA-CELL-045`, and `VBA-CELL-046`.

- Added OOXML container package vector graphics, XSLT, and relationship cloaking security threat inspection rules:
  - **Metafile Exploit & Payload Smuggling (`VBA-CELL-041`, Critical/High)**: Inspects Windows Metafile (WMF/EMF) graphics parts (`*.wmf`, `*.emf`, `/media/`) for CVE-2005-4560 `META_SETABORTPROC` records (`0x052F`), cloaked Windows PE executable headers (`MZ`/`PE`), embedded Windows Shell Link (`.lnk`) shortcuts, embedded shell execution commands (`powershell`, `cmd.exe`, `wscript.exe`, `cscript.exe`, `mshta`, `rundll32`, `certutil`), or remote UNC paths.
  - **XSLT Stylesheet & Transform Script Injection (`VBA-CELL-042`, Critical/High)**: Inspects XML parts and stylesheets (`styles.xml`, `settings.xml`, `customXml/`, `*.xsl`, `*.xslt`) for executable `<msxsl:script>` elements (`language="JScript"` / `"VBScript"`), remote `<w:saveThroughXslt>` transform targets, dangerous XPath `document()` SSRF / NTLM coercion calls, and Windows Shell COM automation objects (`WScript.Shell`, `Shell.Application`).
  - **Relationship Target Cloaking & Protocol Evasion (`VBA-CELL-043`, Critical/High)**: Inspects relationship files (`*.rels`) for null-byte or Unicode bidirectional override cloaking characters (`\0`, `%00`, `\u{202E}`), percent-encoded dangerous URI protocols (`m%73-m%73dt:`, `s%65arch-ms:`, `m%68tml:`, `j%61vascript:`, `v%62script:`, `ms-appinstaller:`), and local IPC named pipe / loopback coercion targets (`\\.\pipe\`, `\\127.0.0.1\`, `\\localhost\`).
- Extended Excel formula evaluation engine (`formula_eval`) with branchless math and DBCS-aware byte string functions:
  - **Branchless Arithmetic & Comparison Logic**: Added `DELTA(number1, [number2])` (Kronecker delta equality test returning 1.0 or 0.0), `GESTEP(number, [step])` (step function testing number $\ge$ step returning 1.0 or 0.0), and `SIGN(number)` (signum function returning 1.0, -1.0, or 0.0) for de-obfuscation of branchless conditional logic.
  - **DBCS-Aware Byte String Functions**: Added `LENB(text)` (double-byte character set byte counting), `LEFTB(text, [num_bytes])`, `RIGHTB(text, [num_bytes])`, and `MIDB(text, start_byte, num_bytes)` with precise DBCS character window slicing, neutralizing evasion attempts targeting East Asian Excel locales.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `DELTA`, `GESTEP`, `SIGN`, `LENB`, `LEFTB`, `RIGHTB`, and `MIDB`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-041`, `VBA-CELL-042`, and `VBA-CELL-043`.

- Added OOXML container package metadata, taskpane, and data connection security threat inspection rules:
  - **Document Property Payload Smuggling (`VBA-CELL-038`, Critical/High)**: Inspects document property parts (`docProps/core.xml`, `docProps/app.xml`, `docProps/custom.xml`) for smuggled Base64 Windows PE binaries, shell execution commands (`powershell`, `cmd.exe`, `wscript.exe`, `cscript.exe`, `mshta`, `rundll32`, `certutil`), dangerous URI schemes (`ms-msdt:`, `search-ms:`, `mhtml:`), or remote UNC paths enabling NTLM credential coercion.
  - **Office Web Extension & Taskpane Auto-Show Anomaly (`VBA-CELL-039`, Critical/High)**: Inspects Office Web Add-in and Taskpane parts (`word/webextensions/`, `xl/webextensions/`, `ppt/webextensions/`, `word/taskpanes/`, `xl/taskpanes/`, `ppt/taskpanes/`) and relationships for automatic display triggers (`canAutoShow="1"`, `visibility="visible"`), remote external web targets, dangerous URI schemes, or embedded script code enabling macro-less execution.
  - **PivotCache Data Connection Anomaly & Shell Injection (`VBA-CELL-040`, Critical/High)**: Inspects workbook PivotCache definitions (`xl/pivotCache/pivotCacheDefinition*.xml`) and relationships for external UNC connection paths for NTLM credential coercion, database shell execution commands (`xp_cmdshell`, `sp_OACreate`, `OPENROWSET`), or dangerous URI schemes.
- Extended Excel formula evaluation engine (`formula_eval`) with Array Inversion & Classic Vector/Array Lookup:
  - **Dynamic Array Inversion**: Added `TRANSPOSE(array)` supporting multi-dimensional range and array literal transpositions with dimension validation and scalar preservation.
  - **Classic Vector & Array Lookup**: Added `LOOKUP(lookup_value, lookup_vector, [result_vector])` and `LOOKUP(lookup_value, array)` supporting 2-argument wide/tall array resolution and 3-argument vector approximate lookups for de-obfuscation pipelines.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `TRANSPOSE` and `LOOKUP`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-038`, `VBA-CELL-039`, and `VBA-CELL-040`.

- Added OOXML container package & obfuscation threat inspection rules:
  - **Glossary Document Payload & Template Injection (`VBA-CELL-035`, Critical/High)**: Inspects Word glossary document parts (`word/glossary/document.xml`, `word/glossary/settings.xml`) and relationships (`word/glossary/_rels/document.xml.rels`) for remote external template injection (`attachedTemplate`), dangerous protocol handlers (`ms-msdt:`, `search-ms:`, `mhtml:`), executable targets, field codes executing DDE/shell commands (`DDE`, `DDEAUTO`, `EXEC`), remote document injection (`INCLUDETEXT`), and smuggled AltChunk/subdocument references.
  - **Embedded Font Obfuscation & Payload Smuggling (`VBA-CELL-036`, Critical/High)**: Inspects package font table relationships (`_rels/fontTable.xml.rels`) for external UNC paths enabling NTLM credential coercion, and embedded font streams (`.odttf`, `.ttf`, `.woff`) for smuggled Windows PE binaries, Windows Shell Link shortcuts (`.lnk`), ELF/Mach-O binaries, staged scripts, or obfuscated ODTTF font streams that de-mask to PE executables via reversed GUID XOR keys.
  - **Digital Ink Definition Anomaly & Action Triggers (`VBA-CELL-037`, Critical/High)**: Inspects Digital Ink markup parts (`xl/ink/ink*.xml`, `word/ink/ink*.xml`, `ppt/ink/ink*.xml`, `ink*.bin`, `ink*.isf`) and relationships for interactive action/hover click triggers (`<a:hlinkClick>`, `<a:hlinkHover>`, `ppaction://program`) executing external programs, remote UNC coercion targets, weaponized CLSIDs, or cloaked PE binaries in binary ink streams.
- Extended Excel formula evaluation engine (`formula_eval`) with Excel 365 Dynamic Array Sequence & Single item extraction:
  - **Dynamic Array Sequence Generation**: Added `SEQUENCE(rows, [columns], [start], [step])` generating sequential 1D and 2D number arrays with dimension validation and memory bounds.
  - **Implicit Intersection & Single Extraction**: Added `SINGLE(value)` extracting the first scalar item from array constants or multi-cell ranges.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `SEQUENCE` and `SINGLE`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-035`, `VBA-CELL-036`, and `VBA-CELL-037`.

- Added OOXML container package & worksheet evasion security threat inspection rules:
  - **Smuggled Container Payload (`VBA-CELL-032`, Critical/High)**: Inspects all container package entries for smuggled standalone executable and script extensions (`.exe`, `.dll`, `.sys`, `.scr`, `.bat`, `.cmd`, `.ps1`, `.vbs`, `.hta`, `.lnk`, `.iso`, etc.), Windows PE binary signatures (`MZ` + `PE\0\0`) cloaked under non-executable extensions (e.g. `.png`, `.jpg`, `.xml`, `.bin`), Windows Shell Link (`.lnk`) binary shortcut files, ELF / Mach-O Unix binaries, and staged script files (`#!/bin/`, powershell invocations).
  - **Worksheet View Evasion & Cloaked Grid (`VBA-CELL-033`, High/Medium)**: Inspects worksheet view definitions (`xl/worksheets/sheet*.xml`) for cloaked formula payloads embedded in hidden columns (`<col hidden="1"/>` or `width="0"`), hidden rows (`<row hidden="1"/>` or `ht="0"`), extreme viewport displacement (`topLeftCell` row $\ge$ 1000 or column $\ge$ 50), and cloaked grid headers (`showGridLines="0"` combined with `showRowColHeaders="0"`).
  - **ActiveX Object Declaration Anomaly (`VBA-CELL-034`, Critical/High)**: Inspects ActiveX object XML parts (`word/activeX/activeX*.xml`, `xl/activeX/activeX*.xml`, `ppt/activeX/activeX*.xml`) for weaponized CLSIDs (`WScript.Shell`, Equation Editor 3.0, `XMLHTTP`, `Shell.Explorer`, `Scriptlet.TypeLib`, `ADODB.Stream`), remote UNC paths, executable file references, or dangerous URI schemes (`ms-msdt:`, `search-ms:`, `javascript:`, etc.).
- Extended Excel formula evaluation engine (`formula_eval`) with Excel 365 Dynamic Array Stacking:
  - **Dynamic Array Stacking**: Added `VSTACK(array1, [array2, ...])` (vertical matrix concatenation with `#N/A` column padding) and `HSTACK(array1, [array2, ...])` (horizontal matrix concatenation with `#N/A` row padding), integrated with resource-bounded evaluation limits.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `VSTACK`, `HSTACK`, `IFS`, `SWITCH`, and `XOR`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-032`, `VBA-CELL-033`, and `VBA-CELL-034`.

- Added OOXML container package & workbook security threat inspection rules:
  - **External Link Target Anomaly & Hidden OLE/DDE Links (`VBA-CELL-029`, Critical/High)**: Inspects external link caches (`xl/externalLinks/externalLink*.xml`) and relationship files for remote UNC paths, dangerous executable targets (`.exe`, `.bat`, `.cmd`, `.ps1`, `.vbs`, `.hta`), exploit protocols (`ms-msdt:`, `search-ms:`, `mhtml:`), and hidden DDE/OLE server link definitions (`<ddeLink ddeService="...">`, `<oleLink>`).
  - **Web Settings Remote Frameset & Reload Triggers (`VBA-CELL-030`, Critical/High)**: Inspects Word web settings (`word/webSettings.xml`) and relationships for remote framesets (`<w:frameset>`, `<w:frame>`), automatic reload triggers (`<w:reload/>`), script tags, or dangerous exploit URI protocol handlers rendered in web layout view.
  - **Workbook Protection Evasion & Concealed Sheets (`VBA-CELL-031`, High/Medium)**: Inspects workbook definitions (`xl/workbook.xml`) for structure locking (`lockStructure="1"`) combined with veryHidden worksheets (`state="veryHidden"`) used to cloak malicious payloads and evade inspection, as well as dummy/tampered workbook protection password hashes.
- Extended Excel formula evaluation engine (`formula_eval`) with Excel 365 Lambda Helper Functions and native array constant literals:
  - **Excel 365 Lambda Helper Functions**: Added `MAP(array1, [array2, ...], lambda)` (multi-array element-wise mapping), `REDUCE([initial_value], array, lambda)` (accumulator folding across arrays for payload de-obfuscation), `SCAN([initial_value], array, lambda)` (running prefix accumulation returning same-shape array), `BYROW(array, lambda)` (row-by-row sub-vector aggregation), `BYCOL(array, lambda)` (column-by-column sub-vector aggregation), `MAKEARRAY(rows, cols, lambda)` (1-indexed matrix generation with dimension bounds), and `ISOMITTED(parameter)` (optional lambda parameter test).
  - **Native Array Constant Literals**: Added parser and evaluator support for inline array syntax `{val1, val2; val3, val4}` with comma column delimiters and semicolon row delimiters, integrated directly into formula evaluation, dynamic lookup, and lambda helper processing.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `MAP`, `REDUCE`, `SCAN`, `BYROW`, `BYCOL`, `MAKEARRAY`, and `ISOMITTED`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-029`, `VBA-CELL-030`, and `VBA-CELL-031`.

- Added OOXML container package security threat inspection rules:
  - **Custom UI Ribbon Callback Auto-Execution (`VBA-CELL-026`, Critical/High)**: Inspects Custom UI Ribbon XML parts (`customUI/customUI*.xml`) and relationships for `onLoad="..."` automatic execution callbacks triggered upon document open, control `onAction="..."` macro invocation callbacks, shell command keywords, and dangerous exploit protocol handlers.
  - **Legacy Excel 5.0/95 Dialog Sheet Macro (`VBA-CELL-027`, Critical/High)**: Inspects legacy Excel dialog sheets (`xl/dialogsheets/sheet*.xml`) for embedded control macro bindings (`<x:FmlaMacro>`), dialog controls, and auto-popup social engineering lure dialogs.
  - **Content Types Anomaly & MIME Spoofing (`VBA-CELL-028`, Critical)**: Inspects `[Content_Types].xml` for directory traversal in PartNames (`/../`, `..\`), dangerous executable MIME types (`application/x-msdownload`, `application/hta`), and MIME extension spoofing (cloaking `vbaProject` or `macrosheet` under image, audio, or text extensions).
- Extended Excel formula evaluation engine (`formula_eval`) with custom lambda functions, radix conversion, and locale-independent parsing:
  - **Bounded Custom LAMBDA Functions**: Added `LAMBDA(param1, [param2, ...], calculation)` with parameter binding, lexical scoping, variable shadowing, invocation inside `LET()`, and immediately invoked lambda desugaring `=LAMBDA(x, y, x + y)(15, 27)`.
  - **Binary, Hexadecimal, and Octal Radix Conversions**: Added `BIN2HEX`, `HEX2BIN`, `OCT2HEX`, and `HEX2OCT` supporting two's complement 10-character binary, negative value handling, and custom padding places.
  - **Locale-Independent Number Parsing**: Added `NUMBERVALUE(text, [decimal_separator], [group_separator])` with custom delimiter normalization and trailing percentage handling.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `LAMBDA`, `BIN2HEX`, `HEX2BIN`, `OCT2HEX`, `HEX2OCT`, and `NUMBERVALUE`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-026`, `VBA-CELL-027`, and `VBA-CELL-028`.

- Added OOXML container package security threat inspection rules:
  - **Word Field Code Execution (`VBA-CELL-023`, Critical/High)**: Inspects Word document XML parts (`word/document.xml`, headers, footers, footnotes, endnotes, comments) for simple fields (`<w:fldSimple w:instr="...">`) and complex fields (`<w:fldChar>` + `<w:instrText>` runs) executing commands via `DDE`/`DDEAUTO`, injecting remote documents or coercing NTLM credentials via `INCLUDETEXT`/`LINK`, triggering lure macros via `MACROBUTTON`, leaking UNC host paths via `FILENAME \p`, or invoking dangerous URI schemes (`ms-msdt:`, `search-ms:`, `mhtml:`, etc.).
  - **PowerPoint Slide Action (`VBA-CELL-024`, Critical/High)**: Inspects PowerPoint presentation parts (`ppt/slides/slide*.xml`, layouts, masters, notes, presentation) for clickable and mouse-over hover triggers (`<a:hlinkClick>`, `<a:hlinkHover>`) executing external programs (`ppaction://program`), invoking macros (`ppaction://macro`), or linking directly to executable and script files (`.exe`, `.bat`, `.cmd`, `.ps1`, `.vbs`, `.hta`, `.cpl`, `.msi`).
  - **Suspicious Alternative Format Chunk (`VBA-CELL-025`, Critical/High)**: Inspects Word Alternative Format Chunk (`aFChunk`) relationships and container parts (`word/afchunk*.dat|htm|rtf|mht|bin`) for external relationships targeting remote exploit templates, HTML smuggling (`data:text/html;base64,`), embedded `<script>` tags, RTF exploit headers (`{\rtf` with `\objdata`, equation editor CLSIDs), Base64 PE executables, raw `MZ` headers, or dangerous URI protocol handlers.
- Extended Excel formula evaluation engine (`formula_eval`) with scoped variable binding and RFC 3986 URL encoding:
  - **Scoped Variable Evaluation**: Added `LET(var1, val1, [var2, val2, ...], calc)` supporting Excel 365 lexical scoping, sequential binding evaluation, variable shadowing, and robust resolution of variable identifiers matching cell address formats.
  - **Dynamic URL Encoding**: Added `ENCODEURL(text)` performing RFC 3986 percent-encoding of non-alphanumeric characters, enabling automated de-obfuscation of dynamically assembled exfiltration URLs and remote download targets.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `LET` and `ENCODEURL`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-023`, `VBA-CELL-024`, and `VBA-CELL-025`.

- Added OOXML container package & formula security threat inspection rules:
  - **Real-Time Data Execution (`VBA-CELL-020`, Critical/High)**: Detects `=RTD(...)` formulas in worksheet cells and defined names that invoke external COM automation servers (`WScript.Shell`, `Shell.Application`, `Scripting.FileSystemObject`, `System.Diagnostics`, etc.) or remote DCOM servers (`\\host\share`, remote IP addresses) to trigger code execution and NTLM coercion.
  - **Suspicious SVG Vector Graphic (`VBA-CELL-021`, Critical/High)**: Detects embedded SVG graphics (`/media/*.svg`) containing malicious `<script>` tags, inline event handlers (`onload=`, `onerror=`, `onclick=`, `onmouseover=`), XML external entities (XXE), `<foreignObject>` HTML embedding, or dangerous URI protocol handlers.
  - **Tampered VBA Project Signature (`VBA-CELL-022`, High)**: Detects truncated, hollowed, or corrupted digital signature binaries (`vbaProjectSignature*.bin`), invalid ASN.1/PKCS#7 headers, or dangling signature relationships in `.rels` indicating signature stripping or tampering.
- Extended Excel formula evaluation engine (`formula_eval`) with formula inspection, type queries, and Roman numeral conversion:
  - **Formula Inspection**: Added `FORMULATEXT(reference)` enabling cross-cell formula string extraction, dynamic slicing, and recursive de-obfuscation.
  - **Type & Information Queries**: Added `TYPE(value)` (returning Excel standard type numbers: 1 for number, 2 for text, 4 for boolean, 16 for error, 64 for array) and `ISNONTEXT(value)`.
  - **Roman Numeral Conversion**: Added `ROMAN(number, [form])` (Arabic to Roman numeral translation) and `ARABIC(text)` (Roman numeral parsing and numeric decoding).
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `FORMULATEXT`, `ROMAN`, `ARABIC`, `TYPE`, and `ISNONTEXT`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-020`, `VBA-CELL-021`, and `VBA-CELL-022`.

- Added OOXML container package security threat inspection rules:
  - **Suspicious Drawing Action (`VBA-CELL-018`, High)**: Detects drawing shapes, PowerPoint slide actions, and legacy Excel VML form controls configured with mouse-over hover triggers (`<a:hlinkHover>`), external program execution (`ppaction://program`), macro actions (`ppaction://macro`), VML button macro bindings (`<x:FmlaMacro>`), or drawing relationships targeting dangerous executables/scripts or exploit protocol handlers.
  - **External Data Connection (`VBA-CELL-019`, High)**: Detects external data connections, query tables, and Word MailMerge configurations containing remote UNC paths (`Data Source=\\`, NTLM credential coercion vector), web query remote payloads (`type="4"`, `connection="URL;http..."`), or command injection keywords (`xp_cmdshell`, `powershell`, `cmd.exe`).
- Extended Excel formula evaluation engine (`formula_eval`) with dynamic array filtering, sorting, matrix dimension inspection, and wrapping functions:
  - **Dynamic Array Filtering & Sorting**: Added `FILTER(array, include, [if_empty])`, `SORT(array, [sort_index], [sort_order], [by_col])`, `SORTBY(array, by_array1, [order1])`, and `UNIQUE(array, [by_col], [exactly_once])` with complete Excel-compliant row/column filtering, stable value comparison ranking, and deduplication.
  - **Vector Wrapping & Reshaping**: Added `WRAPROWS(vector, wrap_count, [pad_with])` and `WRAPCOLS(vector, wrap_count, [pad_with])` for structured matrix folding from flat arrays.
  - **Grid Dimension & Rounding Functions**: Added `ROWS(array)`, `COLUMNS(array)`, and `MROUND(number, multiple)`.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `FILTER`, `SORT`, `SORTBY`, `UNIQUE`, `WRAPROWS`, `WRAPCOLS`, `ROWS`, `COLUMNS`, and `MROUND`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-018` and `VBA-CELL-019`.

- Added OOXML container package security threat inspection rules:
  - **Suspicious Printer Settings (`VBA-CELL-015`, High)**: Detects printer settings (`printerSettings*.bin`) containing external UNC paths (`\\host\share` in ASCII or UTF-16LE DEVMODEW), remote URLs (`http://`, `https://`), command execution triggers (`powershell`, `cmd.exe`), or external printerSettings OPC relationships (CVE-2023-36884 / Storm-0978 vector).
  - **Custom XML Payload Smuggling (`VBA-CELL-016`, High)**: Detects base64-encoded PE executables (`TVqQAAMAAAAEAAAA...`), XML External Entity (XXE) injection (`<!ENTITY ... SYSTEM`), script/shell tags (`<script`, `WScript.Shell`), and HTML smuggling (`data:text/html;base64,`) inside `/customXml/*.xml` container parts.
  - **Suspicious Protocol Handler (`VBA-CELL-017`, Critical)**: Detects dangerous URI protocol handlers (`ms-msdt:` for Follina CVE-2022-30190, `search-ms:`, `ms-appinstaller:`, `mhtml:`, `javascript:`, `vbscript:`, remote `file:////`) across all `.rels` package relationships.
- Extended Excel formula evaluation engine (`formula_eval`) with modern array manipulation, radix, and text conversion functions:
  - **Radix Conversion & Decoding**: Added `BASE(number, radix, [min_length])` and `DECIMAL(text, radix)` for dynamic decoding of arbitrary-base numeric and hex obfuscation strings.
  - **Equality & Step Thresholding**: Added `DELTA(n1, [n2])` and `GESTEP(n, [step])`.
  - **Modern Dynamic Array Shaping**: Added `TAKE` (head/tail slice), `DROP` (head/tail trim), `CHOOSEROWS` (arbitrary row indexing), `CHOOSECOLS` (arbitrary column indexing), `TOROW` (flatten to row vector), `TOCOL` (flatten to column vector), and `EXPAND` (grid expansion with custom pad value).
  - **Text Formatting & Serialization**: Added `ARRAYTOTEXT(array, [format])` and `VALUETOTEXT(value, [format])`.
  - **Scalar Range Unwrapping**: 1x1 Range evaluation results at the top level of `evaluate_formula` automatically unwrap to their scalar value.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `BASE`, `DECIMAL`, `TAKE`, `DROP`, `CHOOSEROWS`, `CHOOSECOLS`, `TOROW`, `TOCOL`, `EXPAND`, `ARRAYTOTEXT`, and `VALUETOTEXT`.
- Extended SARIF v2.1.0 and structured JSON exports with complete rule descriptors and locations for `VBA-CELL-015`, `VBA-CELL-016`, and `VBA-CELL-017`.

- Extended Excel formula evaluation engine (`formula_eval`) with bitwise operations, date/time calculation, modern lookups, and advanced text manipulation functions:
  - **Bitwise Cryptographic Functions**: Added `BITAND`, `BITOR`, `BITXOR`, `BITLSHIFT`, and `BITRSHIFT` (48-bit unsigned integer range with arithmetic shifts), enabling automated de-obfuscation and decryption of XOR-encrypted shellcode and commands in worksheet cells.
  - **Date & Time Functions**: Added `DATE`, `TIME`, `YEAR`, `MONTH`, `DAY`, `HOUR`, `MINUTE`, `SECOND`, `EDATE`, and `EOMONTH` (with Gregorian component normalization and month-end clamping).
  - **Modern Dynamic Lookups & Inspection**: Added `XLOOKUP` (multi-mode search, reverse direction, wildcard matching, and 2D array returns), `XMATCH` (exact, approximate, wildcard, and reverse search modes), `ISERR`, `ISREF`, and `NA`.
  - **Modern Text Functions**: Added `TEXTBEFORE` (instance selection, case sensitivity, match-end handling), `TEXTAFTER`, and `TEXTSPLIT` (2D multi-delimiter string splitting into range values).
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `BITXOR`, `BITAND`, `BITOR`, `BITLSHIFT`, `BITRSHIFT`, `XLOOKUP`, `XMATCH`, `TEXTBEFORE`, `TEXTAFTER`, and `TEXTSPLIT`, uncovering XOR-encrypted LOLBins, dynamic table lookups, and split DDE commands across workbook cells.
- Extended Excel formula evaluation engine (`formula_eval`) with dynamic grid resolution and reference functions:
  - **Dynamic Reference & Grid Resolution**: Added `ADDRESS` (dynamic cell reference construction with absolute/relative modes and sheet naming), `INDIRECT` (dynamic reference string resolution to cells or ranges, with cross-sheet support), and `OFFSET` (dynamic range shifting with row/column offsets, height, and width).
  - **Reference Chaining**: Added multi-hop recursive reference origin evaluation (`eval_cell_origin`), enabling chained dynamic lookups like `CONCAT(OFFSET(A1, 0, 0, 1, 3))` and `INDIRECT(ADDRESS(1, 1))`.
  - **Multi-Cell Range Aggregation in `CONCAT`**: Extended `CONCAT` to evaluate `EvalValue::Range` arguments across contiguous multi-cell rows and columns.
  - **Advanced Math & Rounding**: Added `CEILING`, `CEILING.MATH`, `FLOOR`, `FLOOR.MATH`, `EVEN`, `ODD`, `FACT` (bounded factorial), `GCD` (greatest common divisor), and `LCM` (least common multiple).
  - **Trigonometry**: Added `DEGREES`, `RADIANS`, `SIN`, `COS`, `TAN`, `ASIN`, `ACOS`, `ATAN`, and `ATAN2`.
- Enhanced cell type handling in formula evaluation lookup to support `inlinestr` and `inline_string` cell types alongside shared strings and numeric values.
- Extended container cell threat scanning (`VBA-CELL-009`) with triggers for `INDIRECT`, `OFFSET`, and `ADDRESS` expressions, de-obfuscating dynamic multi-cell DDE commands and cross-sheet payload URLs.
- Added container-level OOXML package threat scanning (`scan_ooxml_package_threats`) across `.xlsm`, `.docm`, `.pptm`, and macro-less OOXML containers (`.docx`, `.xlsx`), detecting Remote Template Injection (`VBA-CELL-010`), Embedded OLE Packages / Equation Editor droppers (`VBA-CELL-011`), External OLE Object links (`VBA-CELL-012`), Embedded ActiveX controls (`VBA-CELL-013`), and External Subdocument references (`VBA-CELL-014`).
- Enhanced `extract_macro_container` and `inspect_macro_container` with graceful fallback for weaponized macro-less containers (such as Word documents with external template injection or Excel documents with embedded OLE packages without VBA modules), extracting and reporting package threats in JSON and SARIF.
- Extended SARIF v2.1.0 and JSON exports with complete rule definitions, descriptors, and results for `VBA-CELL-010` through `VBA-CELL-014` with logical location mapping for package parts and OPC relationships.
- Extended Excel formula evaluation engine (`formula_eval`) with 18 additional functions:
  - **Radix & Base Conversions**: `HEX2DEC`, `DEC2HEX`, `BIN2DEC`, `DEC2BIN`, `OCT2DEC`, `DEC2OCT` (supporting 40-bit two's complement and padding).
  - **Unicode Encoding**: `UNICHAR` and `UNICODE` (supporting full UTF-8 Unicode code points and surrogate exclusion).
  - **Grid & Table Lookups**: `INDEX` (1D vectors and 2D matrices), `MATCH` (exact and ordered approximate matching), `VLOOKUP` (table vertical lookups), and `HLOOKUP` (horizontal table lookups).
  - **Text & Structure Formatting**: `TEXTJOIN` (multi-argument and range joining with custom delimiter and empty cell filtering), and `PROPER` (title case conversion).
  - **Logic & Mathematics**: `XOR` (parity logic), `PRODUCT` (multi-factor arithmetic aggregate), `QUOTIENT` (integer division), and `LOG` (custom base logarithm).
- Enhanced 2D range matrix preservation in `EvalValue::Range { values, rows, cols }`, enabling accurate multi-row/multi-column grid indexing and table lookups across sheets.
- Extended container cell threat scanning (`VBA-CELL-009`) with dynamic evaluation for radix conversion, Unicode, grid lookup (`INDEX`, `VLOOKUP`, `HLOOKUP`, `MATCH`), and `TEXTJOIN` expressions, proactively exposing obfuscated DDE payloads and remote dropper URLs hidden across workbook grids.
- Expanded Living-off-the-Land Binaries (LOLBins) recognized in cell threat evaluation to include `regsvcs`, `control.exe`, `bash.exe`, and `wsl.exe`.
- Added dynamic Excel formula de-obfuscation in container cell threat scanning (`VBA-CELL-009` `DeobfuscatedThreatFormula`), evaluating formulas with `CHAR()`, `CONCATENATE()`, `&`, `MID()`, `SUBSTITUTE()`, `CHOOSE()`, and `HYPERLINK()` to detect hidden DDE, executable download, and LOLBin payload executions.
- Extended Excel formula evaluation engine (`formula_eval`) with advanced control, string, math, and information functions: `PI`, `EXP`, `LN`, `LOG10`, `ISNONTEXT`, `TYPE`, `CHOOSE` (with lazy evaluation), `IFNA`, `IFS`, `SWITCH`, `CHAR`, `CODE`, `CLEAN`, `T`, `N`, `FIND`, `SEARCH`, and `HYPERLINK`.
- Enhanced VBA Stomping detection (`stomping::detect_vba_stomping`) to identify Windows API callback injection techniques (`EnumChildWindows`, `LineDDA`, `EnumFontFamiliesA`, `EnumSystemLocalesA`, `GrayStringA`, etc.) and additional LOLBins (`hh.exe`, `installutil`, `regasm`, `regsvcs`, `msconfig`, `control.exe`, `bash.exe`, `wsl.exe`, `tar.exe`, `explorer.exe`).
- Refined `.com` executable detection in `HYPERLINK` scanning to distinguish between `.com` executable files and `.com/` TLD domains, eliminating false positives on standard web links.
- Added dangerous worksheet cell threat and workbook structure scanning for Excel macro containers (`scan_workbook_cell_threats`), detecting DDE execution (`VBA-CELL-001`), legacy XLM/Excel 4.0 macro formulas (`VBA-CELL-002`), Remote Workbook Links (`VBA-CELL-003`), `WEBSERVICE`/`FILTERXML` exfiltration formulas (`VBA-CELL-004`), suspicious external hyperlinks (`VBA-CELL-005`), auto-executing defined names (`VBA-CELL-006`), `xlSheetVeryHidden` state evasion (`VBA-CELL-007`), legacy XLM macro sheets (`VBA-CELL-008`), and de-obfuscated threat formulas (`VBA-CELL-009`).
- Added full OASIS SARIF v2.1.0 report generation (`stomping_to_sarif`, `inspection_to_sarif`) embedding comprehensive rule descriptors, help markdown, CWE mappings, physical file URIs, and structured results for seamless GitHub Code Scanning and CI integration.
- Enriched structured JSON exports (`stomping_to_json`, `ContainerInspection::to_json`) with standardized `rule_id` identifiers (`VBA-STOMP-001..010` and `VBA-CELL-001..009`).
- Added CLI `--format sarif` alongside `json`, `markdown`, and `text`, plus automatic Office host profile inference from container extensions (`.docm`/`.dotm` -> Word, `.pptm`/`.ppam` -> PowerPoint, `.accdb`/`.mdb` -> Access, `.xlsm`/`.xlsb` -> Excel).
- Added multi-language bindings APIs:
  - Python ABI3 bindings: `inspect_file_sarif`, `disasm_file`, `disasm_file_json`, `disasm_file_markdown`, `inspect_file_markdown`.
  - Node.js N-API bindings: `inspectMacroFileSarif`, `disasmMacroFileJson`, `disasmMacroFileMarkdown`, `inspectMacroFileMarkdown`.
- Added built-in standard VBA P-code instruction schemas for VBA6 and VBA7 (32-bit and 64-bit) supporting all 264 opcodes without requiring external caller-supplied tables.
- Added `_VBA_PROJECT` stream identifier table parser to resolve internal runtime names and user-defined identifiers.
- Added high-fidelity P-code disassembler (`disassemble_pcode_module`, `disassemble_extracted_project`) producing formatted line-by-line assembly with procedure declarations, string literals, and call targets.
- Added automated VBA Stomping and tampering detection engine (`stomping::detect_vba_stomping`, `extract::detect_project_stomping`) diagnosing purged source, hidden procedures, suspicious literals, and sensitive API call divergence.
- Added direct macro container extraction (`extract_macro_container`) automatically detecting OOXML packages (`.xlsm`, `.xlsb`, `.docm`, `.pptm`) and legacy CFB binaries (`.xls`, `vbaProject.bin`).
- Added end-to-end `inspect_macro_file` API and CLI commands `disasm`, `stomping`, and `inspect`.
- Composed omitted `Optional Variant` arguments per bounded caller callsite and path. `IsMissing` now receives an explicit omission marker for that path, while mixed supplied/omitted or unresolved calls remain separate candidates.
- Added path-local dynamic-array shape snapshots for literal `ReDim` bounds. `LBound`/`UBound` can use a proven shape until an `Erase`, unsupported mutation, incompatible `ReDim Preserve`, or dynamic bound clears it; post-mutation runtime shape stays unresolved.
- Added the opt-in `formula_eval` API for a bounded, side-effect-free Excel formula subset: arithmetic, comparisons, concatenation, static A1 references/ranges, selected aggregate/string functions, and lazy `IF`. Formula cells are never recursively recalculated; dynamic/external references, unsupported functions, locale-sensitive behavior, and resource-limit cases retain explicit unresolved statuses.
- Extended path value substitution through bounded acyclic value-alias chains and let `ReDim` shape facts prove `IsArray` for Variant targets, while cycles and unsupported mutations remain unresolved.
- Added bounded three- and four-hop return-chain composition. Existing two-hop fields remain compatible, while new chain vectors retain every wrapper procedure, path, condition, completion flag, and leaf return value.
- Extended caller-supplied p-code semantics with explicit `Object(identity)` and bounded `Array(values)` abstract values plus `IsObject`/`IsArray` unary predicates. These are schema-selected states; raw operands and runtime object/array payloads remain unknown unless the caller supplies the meaning.
- Caller-assigned p-code object identities now support bounded equality/inequality comparison; ordering and unmodeled runtime identity remain `Unknown`.
- Added caller-supplied `ArrayLength` p-code evaluation for explicit bounded `Array(values)` states; runtime array shape is never inferred.
- Added caller-supplied `ArrayIndex` p-code stack action for bounded zero-based array reads; out-of-range and unknown/runtime arrays remain `Unknown`.

## 0.1.0 — unreleased

- Established a dependency-free Rust library and CLI.
- Added a source lexer/parser, structural CFG, candidate call/data-access findings, JSON/DOT output, bounded ZIP/Deflate and CFB readers, and MS-OVBA source extraction.
- Resolved `.xlsm` VBA Project parts through OPC package and workbook relationships instead of assuming the conventional `xl/vbaProject.bin` location; rejects external, ambiguous, and root-escaping targets.
- Inventoried workbook sheet names, IDs, visibility state, relationship kind, and resolved part targets as extraction metadata; names, states, relationship IDs, and paths follow source disclosure in JSON.
- Extracted workbook sheet names, visibility state, IDs, relationship kinds, and internal part targets as source-disclosure-aware metadata, without claiming runtime worksheet or cell resolution.
- Linked literal name and positive integer `ThisWorkbook.Worksheets(...)` / `ThisWorkbook.Sheets(...)` lookups, including explicit `.Item(...)`, to candidate facts in the extracted workbook inventory; dynamic selectors, unqualified collections, and other workbook receivers remain unresolved.
- Linked worksheet-qualified `Range(...)`, `Cells(row, column)`, and `Cells(index)` calls to saved sheet-name/index candidates, preserving literal selectors under source disclosure without claiming live cell values or effects.
- Added exact-span links from worksheet-qualified access candidates into associated read/write paths, assignment value-flow candidates, and decision predicates.
- Extracted SpreadsheetML defined-name names, formulas, hidden/built-in status, and local sheet scope; linked literal `ThisWorkbook.Names(...)` and `Range("Name")` lookups as candidates without evaluating the defined-name formula.
- Linked `Name.RefersToRange` to a worksheet/range candidate only when the saved defined-name formula is a simple static A1 reference; formulas, constants, external references, and unsupported expressions remain unresolved.
- Added source-disclosure-aware one-based coordinate bounds for simple A1 cells/rectangles, whole-row/whole-column ranges, and literal `Cells(row, column)` selectors; multi-area unions and intersections stay unresolved.
- Added caller-supplied p-code semantic stack evaluation on top of decoded line instructions. Version-specific action definitions can propagate literal operands, apply bounded integer arithmetic/comparison, preserve unknown values, and record stack underflow plus branch/Call/Return candidates; the report remains explicitly unverified and does not bundle or infer an Office opcode table.
- Extended the p-code semantic layer with caller-defined local-slot load/store state and disclosure-aware JSON snapshots.
- Added a bounded p-code byte-payload concatenation semantic action that preserves overflow as `Unknown`.
- Added explicit semantic string values and bounded string concatenation without inferring text from raw byte payloads.
- Added conservative path folding for unshadowed integer intrinsics `Abs`, `Fix`, `Int`, `Sgn`, `CByte`, `CInt`, and `CLng`.
- Added exact-integer-square folding for unshadowed `Sqr` path predicates.
- Added conservative integer/zero-digit folding for unshadowed `Round` path predicates.
- Added bounded Date path values for explicit date literals, `DateSerial`, `TimeSerial`, `DateAdd` day/time/month/quarter/year intervals with Gregorian month-end clamping, `DateDiff` intervals, `DatePart` fields, `DateValue`/`TimeValue` explicit text, `CDate` serials, and Date arithmetic/comparisons.
- Added bounded literal/constant folding for `IIf`, `Choose`, and `Switch`, requiring both `IIf` result operands to be safe because VBA evaluates them eagerly.
- Added ASCII-safe path folding for `CStr`, `Val`, and integral string forms of `CByte`/`CInt`/`CLng`.
- Added bounded `LBound`/`UBound` path folding for fixed declared arrays with resolvable literal/Const bounds and dimensions.
- Added rank-one nonempty `Array(...)` literal bound folding when elements have no unsafe calls.
- Added `LBound(ParamArray)` zero-base folding and exact-slot `UBound(ParamArray)` folding for bounded positional callsites; empty, named, dynamic, and unresolved calls remain runtime-dependent.
- Added ASCII/bounded `Chr`, `Asc`, `Space`, and `String` intrinsic path folding.
- Added nonnegative integral `Hex` and `Oct` intrinsic path folding.
- Extended caller-supplied p-code semantics with Boolean/integer `Xor`, `Eqv`, and `Imp` plus integer bitwise `And`/`Or`.
- Added standalone validation for caller-supplied p-code semantic schemas.
- Added standalone validation for caller-supplied p-code instruction schemas.
- Added standalone line-map metadata validation before p-code decoding.
- Preserved valid observed no-code line records during line-map validation.
- Added bounded p-code semantic path exploration for caller-defined relative branches, preserving fall-through/target paths and explicit loop, unknown-instruction, and step-limit termination states.
- Added caller-defined conditional relative branches that select a known Boolean/integer target or retain both paths for unknown conditions.
- Added caller-selected UTF-8/UTF-16LE string operand actions to the abstract p-code semantic layer; raw bytes remain uninterpreted unless the schema explicitly selects an encoding.
- Added finite IEEE-754 `Float64Bits` unary/binary arithmetic and comparison to caller-supplied p-code semantics, including integer/float numeric promotion; NaN, infinities, and unsupported type combinations remain `Unknown`.
- Added `CallKnownReturn` for caller-supplied p-code schemas that model a verified intrinsic/call summary without executing a callee body.
- Added caller-schema p-code Binary string ordering comparisons alongside equality and bounded concatenation.
- Added explicit p-code `Null`/`Empty` semantic values with bounded Null propagation and Empty-to-zero arithmetic coercion.
- Added p-code `Error(code)` values and `PushErrorOperand`; equal error codes compare, while arithmetic and mixed error operations remain Unknown.
- Added semantic-report counters for unknown values, unknown instructions, and stack underflow.
- Added the same incompleteness counters to bounded semantic path reports and their JSON export.
- Added an extraction-layer convenience API that runs caller-supplied p-code decode and semantic path analysis directly on an extracted module line map.
- Added `analyze_xlsm_with_pcode_profile` to combine direct workbook extraction, VBA analysis, and selected compiled-cache line-map metadata.
- Added the equivalent p-code profile extraction helper for raw VBA project binaries.
- Added an end-to-end `.xlsm` API returning VBA analysis, extraction metadata, and bounded caller-supplied p-code semantic reports together.
- Added the raw VBA project binary equivalent for end-to-end semantic and compiled-representation analysis.
- Added `PCodeAnalysisOptions` and options-struct convenience wrappers for end-to-end callers.
- Added matching options-struct wrappers for extracted module/project p-code analysis.
- Added project-level p-code semantic JSON export with per-module disclosure and truncation metadata.
- Added path-specific `ParamArray` argument slot snapshots for bounded positional calls.
- Added the positional `ParamArray` slot to source data-flow facts and exact-slot filtering, so duplicate textual actuals cannot cross-associate; bounded caller/callee composition now retains ParamArray slots even when array contents stay unknown.
- Added caller call indices and path-specific Optional Variant omission tracking for bounded `IsMissing` evaluation.
- Extended scalar ByRef write-value composition through bounded nested forwarding calls with an explicit composition depth.
- Added bounded two-hop return-path composition for caller → wrapper → leaf function chains, retaining all three path identities and conditions with a separate truncation flag and conservative feasibility label.
- Added a bounded literal `resolved_return_value` to direct and two-hop return-path facts when a callee return expression is statically evaluable, including wrapper-parameter snapshot reinjection into a leaf call and post-return predicate feasibility; dynamic/user-function returns remain source expressions with unresolved values.
- Added bounded scalar ByRef write-value composition from caller snapshots into simple callee write expressions, while keeping ByVal/ParamArray/parenthesized value passing separate and conservative.
- Added bounded path-local tracking for statically object-compatible `Set target = source` aliases and type predicates such as `IsObject`.
- Added separate path-conditioned typed-member dispatch candidates for simple object aliases, preserving the original call facts and unresolved dynamic calls.
- Added bounded declared-array-to-Variant alias folding for `IsArray` predicates.
- Extracted bounded worksheet cell records, shared/inline strings, formula text and stored formula cache values; linked populated cells to static worksheet/range candidates without recalculating formulas.
- Added bounded formula-reference candidates for a subset of A1 cell/range operands, linking formulas to matching populated-cell records while preserving explicit unresolved status for external and 3-D references.
- Exposed formula-reference scan status so absent shared-formula follower text is not mistaken for a formula with no dependencies.
- Translated supported relative/absolute A1 reference operands from a unique shared-formula master into its follower cells, preserving the source formula cell ID and leaving malformed or out-of-range groups unresolved.
- Matched unqualified formula-name operands to the workbook defined-name inventory, preferring a unique current-sheet name over a workbook-scope name; qualified names and ambiguous/invalid scope cases remain explicit candidates.
- Scanned absolute, scope-resolvable A1 operands and name operands inside each defined-name formula as separate candidates, linking populated cell references without inlining or evaluating named formulas.
- Flagged `INDIRECT` and `OFFSET` formula calls as dynamic-reference candidates while keeping their runtime target unresolved.
- Extracted worksheet table definitions via OPC table relationships and resolved a bounded set of structured table selectors to populated-cell candidates, including data, header, totals, all-table, and current-row forms.
- Linked literal Excel `ListObjects`/`ListColumns` calls and `DataBodyRange`, `Range`, `HeaderRowRange`, and `TotalsRowRange` accesses to saved table/column/cell candidates and terminal data-access paths.
- Linked literal `ListRows(index).Range` expressions to data-row cell candidates while excluding table headers/totals.
- Added `ListRows.Add` append/insert mutation candidates with static position checks and unresolved runtime row identities.
- Linked literal `ListRow.Delete` and `ListObject.Delete` calls to affected saved row/table cell candidates without projecting post-deletion workbook state.
- Added `ListObject.Resize` mutation candidates for literal A1 ranges explicitly qualified to the same saved worksheet; dynamic or cross-sheet targets remain unresolved.
- Rejects partially consumed or unbalanced expression syntax; distinguishes bang dictionary access, bracketed keys, and the `!` Single suffix, and resolves simple string-key access through explicit in-project default members.
- Preserves bracketed identifier delimiters when rendering source fragments and derives parsed assignment inputs from expression trees, keeping dictionary/member names out of receiver-value provenance.
- Separates known procedure callees from assignment-value inputs while retaining declared array receivers and actual arguments as candidate sources.
- Keeps standard-module qualifiers out of value-input candidates while retaining a qualified public module value as `Module.Member`.
- Linked data-transfer and Excel data-access candidates to bounded structural paths with the conditions preceding each operation; path feasibility remains unproven and associations are resource-capped.
- Captured identifier reads in parsed branch, Select Case, loop, and With expressions as path-flow candidates, allowing preceding definitions and call arguments to reach decision uses.
- Derived path-specific simple-variable definition-use candidates so assignments from opposite branches stay separate; member and array aliasing remain unresolved.
- Carried known direct `ByRef` mutation and function-return candidates into path-local value links without claiming full call-chain resolution.
- Connected same-statement Excel read/write candidates to visible simple-variable assignments without claiming workbook or cell identity.
- Linked Excel reads in If, Select Case, and loop tests to their enumerated decision outcomes, keeping predicate text behind the source-disclosure option.
- Processed `#Const` directives inside excluded conditional-compilation blocks, ignored typed-name suffixes, stripped directive comments, and diagnosed repeated module conditional-constant names.
- Expanded conditional-compilation expression evaluation with VBA precedence, `^`, `\`, `&`, `Empty`/`Null`/`Nothing`, a conservative subset of intrinsic calls, and unknown handling for unsupported/locale/type-sensitive cases. Conditional `\`/`Mod` now Long-coerce supported plain numeric operands with banker's rounding, truncate integer quotients toward zero, and use the MS-VBAL absolute remainder for negative `Mod` cases.
- Evaluated the `+` operator as string concatenation when both conditional-compilation operands are known strings; mixed and ambiguous operand types remain unresolved.
- Evaluated bounded ASCII/Binary `Like` patterns with `*`, `?`, `#`, character lists and ranges; locale-dependent, non-ASCII, malformed or over-budget patterns remain unresolved.
- Added compile-time numeric literal suffix and radix handling for the supported MS-VBAL ranges, including D/E exponents and LongLong target gating.
- Applied module `Option Compare Binary` ordering to known ASCII string comparisons; Text/Database locale collation and non-ASCII binary representations remain unresolved.
- Evaluated fully specified English month-name date/time tokens, in-range numeric `CDate` serials, and supported Date arithmetic (`+`, `-`, `*`, `/`, `^`, unary `-`) in conditional-compilation expressions; locale-sensitive string coercions, Date integer division/Mod, logical operations and overflow cases remain unresolved.
- Evaluated `LenB` byte counts for known Unicode strings from their UTF-16 representation while leaving ANSI/DBCS conversions and user-defined-type layouts unresolved.
- Added conservative path folding for `Len`/`LenB` when strings, fixed-length string sizes, or supported scalar storage widths are known; dynamic payloads, UDT layouts, and ANSI/DBCS results stay unresolved.
- Folded callee parameter predicates when every direct call to a private standard-module `ByVal` parameter supplies the same compatible static literal/constant; mutable caller state, `ByRef`, dynamic entries, and uncertain conditional branches stay unresolved.
- Propagated that bounded `ByVal` seed into function return-path associations so return candidates retain callee branch feasibility (including impossible return branches) without claiming full caller/callee path composition.
- Folded `InStr` searches for known ASCII strings under binary comparison, including explicit starts and `vbBinaryCompare`; text/database collation and non-ASCII or runtime values stay unresolved.
- Added bounded `InStrRev` path folding for known ASCII strings, binary comparison, reverse-search positions, and explicit start boundaries; dynamic and collation-dependent cases remain unresolved.
- Added conservative `Left`/`Right`/`Mid` path folding for known ASCII strings and bounded count/start arguments, retaining `Null` and leaving dynamic, non-ASCII, and invalid-bound cases unresolved.
- Added `Trim`/`LTrim`/`RTrim` path folding for known ASCII strings, preserving `Null` and leaving dynamic or non-ASCII values unresolved.
- Added bounded three-argument `Replace` path folding for known ASCII values in Binary compare mode, with a 16 KiB result budget.
- Linked Excel reads in decision nodes to each enumerated structural outcome edge; feasibility remains unproven.
- Added opaque performance-cache metadata reporting; compiled-code disassembly and full VBA semantic coverage remain unimplemented.
- Added direct library entry points for extracting and analyzing `.xlsm` data and already-extracted projects.
- Preserved structured MS-OVBA registered-library, referenced-project, and ActiveX-control reference metadata; parsed valid Libid and ProjectReference identity fields and disclosure-gated machine paths and display names.
- Uses `VB_Name` class/module header attributes for canonical module identity and cross-module references.
- Expanded scope-aware call and value resolution, event/external declaration signatures, named argument validation, array/`ParamArray` checks, and target-aware `PtrSafe` diagnostics. Added `VBA2077`–`VBA2079` checks for shared Property Get/Let/Set index signatures (excluding the setter value parameter and allowing different Optional defaults), Let/Get value type agreement, Property Set object-compatible value types, and missing setter values and Optional/ParamArray/array value-parameter shapes; Optional index defaults may differ, setter value parameters are excluded from the shared index signature and from index `ParamArray` contents, and `VBA2136` rejects property names shared with module variables/constants, Enum members, or external declarations; unknown type-library contracts remain unresolved. `VBA2137` rejects Sub/Function names that collide with module variables/constants or Enum members and external Declare names that duplicate module values or each other. `VBA2138` leaves unqualified public procedure references unresolved with an error when the procedure name matches a project or module name. `VBA2140` rejects Function/Property Get parameters that shadow the implicit result variable. Resolved calls with unmapped required parameters now receive `VBA2020` errors, matching static invocation incompatibility. An omitted positional slot mapped to a required parameter now raises a `VBA2022` error even when a later named argument maps that parameter; omitted optional slots and ParamArray element placeholders remain distinct.
- Applied MS-VBAL explicit `Call` argument rules, diagnosing and excluding unparenthesized trailing arguments from call arity.
- Validated in-project `WithEvents` targets and handler signatures, linked positional `RaiseEvent` calls to possible sinks, carried event argument and `ByRef` write candidates, and checked event declaration/module scope.
- Preserved per-member `VB_UserMemId` values, resolved indexed in-project default-member call candidates and argument flows, inferred zero-argument default getter values inside scalar assignment expressions, and diagnosed known mismatches; external TypeLib defaults remain unresolved.
- Retained class-level predeclared/global namespace attributes, resolved explicitly predeclared class names as typed default instances, diagnosed `Set` assignment to a named predeclared instance, and rejected `VB_GlobalNameSpace=True` on VBA source-project modules.
- Narrowed `CallByName` to in-project member candidates for typed receivers, literal member names, and known `vbCallType`, carrying argument and direct function-return flow candidates.
- Recorded Excel `Application.Run` macro-invocation candidates, resolving only literal or literal-concatenated names to public standard-module `Sub`/`Function` candidates; active-sheet/workbook/runtime dispatch and dynamic names remain unresolved, and named arguments are diagnosed as `VBA2141`.
- Recorded Excel `Application.OnTime` schedule/cancel call candidates and possible scheduled entry points for literal names resolving to public zero-argument standard-module procedures; missing required `EarliestTime`/`Procedure` arguments receive `VBA2142`, while runtime schedule state and exact cancellation matching remain unresolved.
- Extracted SpreadsheetML workbook and worksheet codenames and used them to classify renamed document modules for Excel event entry-point analysis; codename fields follow the source-disclosure policy.
- Validated the documented signatures of selected Excel Workbook/Worksheet events (Sub kind, arity, and declared types), diagnosed mismatches with `VBA2143`, and excluded known invalid signatures from event-entry candidates; unlisted events and parameter passing modes remain unresolved.
- Recognized selected Excel `Application` event handlers on class-level `WithEvents Application` declarations; matching handlers become host-entry candidates, while known signature mismatches receive `VBA2144`.
- Added a dedicated MS-VBAL `TypeOf...Is` expression with Boolean typing, separate object-value/type-reference flow, in-project exact-class/`Implements` compatibility candidates, a known-scalar operand error (`VBA2145`), and a direct Nothing runtime error 91 warning (`VBA2146`); runtime object values and external interfaces remain unresolved.
- Diagnosed ordinary `Is` reference comparisons with known scalar or UDT operands as `VBA2147`, while preserving the separate `TypeOf...Is` type-test form and runtime-unknown Object/Variant cases.
- Expanded caller-schema p-code operand framing with byte, signed/unsigned 16/32-bit, raw 64-bit integer/IEEE-754 bits, and existing length-prefixed byte payload encodings; no opcode assignments are bundled.
- Preserved sequential `ElseIf` flow and procedure-local label resolution in control-flow facts.
- Changed `Select Case` CFG construction to test clauses in source order, enforce first-match flow and route the unmatched remainder to `Case Else`/no-match; invalid non-final or duplicate `Case Else` clauses are diagnosed.
- Parsed each `Select Case` value, inclusive range, and `Is` comparison alternative separately; CFG tests now preserve alternative order and source spans, and JSON follows the source-disclosure policy for their expressions.
- Added `LBound`/`UBound` checks for call arity, known scalar arguments, and constant dimensions outside a statically declared array's rank; unknown runtime ranks remain unresolved.
- Added `For Each` static checks for array control-variable type, explicit `Next` name identity, and in-project UDT element arrays.
- Added static type checks for known `For` loop counters and explicit `Next` variable identity.
- Connected uniquely visible local, same-module, and public standard-module `Const` initializers to path-value flows while respecting local shadowing and ambiguous names.
- Resolved unique qualified module-level `Const` provenance carried through known function-return candidates, independent of source-file order; ambiguous module names remain unlinked.
- Followed simple function-return dependencies through a uniquely defined local variable or local constant to its parameter or module source, with cycle protection and a 32-level bound; locals with distinct possible definitions remain scope-qualified and unresolved.
- Composed local-call and direct-return candidate summaries after call collection, so wrapper return sources are independent of module input order; ambiguous local call results remain scoped.
- Linked direct resolved procedure arguments across caller/callee paths to reads of the matching formal parameter, keeping each side's path conditions and feasibility separate.
- Added caller/callee path links from direct function-return candidates to the callee's return-value assignments, without claiming cross-procedure path feasibility.
- Added nested `Function`/`Property Get` return-source candidates when their results are passed as actual call arguments to a known in-project procedure.
- Preserved nested return candidates from both `IIf` result arguments, matching VBA's eager evaluation semantics.
- Exported the source callee module/procedure on data-flow facts only when source disclosure is enabled.
- Preserved comma-separated nested `Next` variables one enclosing loop at a time.
- Parsed `For` start/end/`Step` expressions and diagnosed only known array, UDT, `LongLong`, and `Nothing` values that cannot be Let-coerced to `Double`; string, `LongPtr`, and host-dependent conversions remain unresolved.
- Applied the MS-VBAL Null selector rule so literal `Select Case Null` reaches only `Case Else` or the no-match continuation.
- Parsed `On...GoTo` and `On...GoSub` dispatch lists, validated every procedure-local target, and added computed-branch and zero/out-of-list fallthrough CFG candidates; coercion errors and input feasibility remain unresolved.
- Added colon-optional numeric line labels, leading-zero canonicalization, and the MS-VBAL 0–2,147,483,647 label range check.
- Expanded direct and computed GoSub control flow with a bounded LIFO resumption-state graph (64 stack frames / 100,000 states); the error-policy pass carries the GoSub stack across handler transfers and Resume, while exceptional-site feasibility and merged CFG edges remain incomplete.
- Added typed-object call/property candidates, basic function-return data-flow candidates, and VBA file I/O/filesystem operation candidates.
- Retained class-module `Implements` relationships and checked procedure signatures plus simple property accessor contracts against in-project interface prototypes.
- Exposed concrete in-project `Implements` targets as runtime dispatch candidates for interface-typed object calls.
- Added conservative state-dependent possible-fault and `Resume` edges for procedures that use `On Error`; the CFG remains incomplete because actual fault sites are unknown.
- Added warning `VBA2122` when propagated procedure error state reaches `Resume` without an active error, which can raise VBA runtime error 20; path feasibility remains unproven.
- Added warning `VBA2123` when CFG GoSub-stack states or the structural CFG show a reachable `Return` without a matching active GoSub, which can raise runtime error 3; path feasibility remains unproven.
- Retained array declaration and `ReDim` dimensions, each `Erase` target, and array writes through `ByRef`; invalid base directives leave implicit bounds unresolved, and structure-only JSON redacts bound expressions.
- Added static diagnostics for `ReDim`/`Erase` on known fixed arrays, scalar values, `ParamArray`, and disallowed `As New` clauses; Variant runtime array state remains unresolved.
- Added conservative `ReDim Preserve` shape checks for adjacent `ReDim` statements, with warnings for known rank/lower/non-final-bound changes and uncertain expression comparisons.
- Parsed module-local `DefType` rules, diagnosed overlapping/misplaced ranges, and applied effective implicit types to variable declarations and assignment type propagation; type suffixes keep precedence and unknown conditional branches leave the mapping unresolved.
- Diagnosed known exact-type mismatches for scalar `ByRef` arguments on project procedure and external `Declare` calls, including uniquely resolved in-project UDT identity, plus array/scalar shape and built-in element-type mismatches on direct array `ByRef` arguments. Uniquely resolved in-project UDT/class array element identities are compared; external or ambiguous type identities, array rank, Variant runtime shape, and unresolved fixed-length String constants remain unresolved; direct scalar and array-element fixed-length String `ByRef` sizes are compared when resolvable.
- Diagnosed known non-object arguments passed to class/`Object` parameters (`VBA2126`), while keeping different-object copy-back behavior and external types unresolved.
- Added `VBA2127` for statically invalid UDT Let-coercions in scalar assignments, whole-array-to-UDT/Variant assignments, and ByVal/temporary call arguments, including different UDT identities and known non-UDT source or destination types; Variant and external-reference behavior follows explicit unresolved rules.
- Added warning `VBA2128` for explicit Let-coercion from `Nothing`, which can raise VBA runtime error 91; `Set ... = Nothing` and object-compatible parameter passing remain distinct.
- Added warning `VBA2131` for explicit `Null` Let-coercion, including omitted Optional defaults, with destination-specific runtime error 13 (UDT/resizable array) or 94 (other non-Variant, non-fixed-array target).
- Added `VBA2129` for statically known non-object Let sources assigned to Object/class targets; Variant and object/class sources remain open-world because runtime value/default-member semantics may apply.
- Added `VBA2130` for known array Let-coercions that violate MS-VBAL destination/element-type rules, leaving Byte-array conversion, Variant runtime shape, and fixed-bound equivalence unresolved.
- Added `VBA2132` for class Let-coercions whose local, accessible zero-argument default getter is missing or returns a type statically incompatible with a scalar assignment or non-Object/non-Variant `ByVal` target; recursive, ambiguous, external, and Variant-result cases remain unresolved.
- Added `VBA2134` for `Set` assignments with statically known non-object targets or non-object values, while leaving Variant contents and cross-class runtime compatibility unresolved.
- Added warning `VBA2135` for direct ASCII string literals without digits that are Let-coerced in assignments, known `ByVal` calls, omitted Optional defaults, or `For` bounds converted to `Double`; digit-bearing locale-dependent formats, punctuation, Date parsing, and overflow remain unresolved.
- Added `VBA1041` for empty Enum declarations, `VBA1042` for duplicate member names, `VBA1044` for member conflicts with module-level variables/constants, `VBA1045` for self, later-member, and later-Enum-declaration references, `VBA1046` for integral decimal initializers outside Long range, and `VBA1047` for floating/Currency initializers outside Long range. Added `VBA1048` for procedure calls in Enum constant expressions. Added `VBA1049` for resolved module-variable references in Enum initializers and `VBA1050` when a `Global Enum` appears in a class-like module; standard-module `Global Enum` declarations retain their public-compatible visibility. Added `VBA1051` for public Enum type-name collisions across the project, private Enum conflicts within a module, and project/library/module name collisions; public Enum/public UDT conflicts continue to use `VBA1034`. Unique public project Enum-member references (including members of public Enums declared in class modules) now resolve within bounded fixed-point passes (128 passes, 100,000 member visits per pass); private cross-module members, ambiguous names, and cycles remain unresolved. Direct floating/Currency literals and bounded Single/Double/Currency Const chains with supported numeric literals or arithmetic use banker's rounding. Integral expressions, uniquely resolved module `Const` and earlier same-module Enum-member references, and implicit sequences expose `enum_value`; `VBA1043` reports implicit Long overflow, and `VBA2086` rejects statically known LongLong initializers. Added bounded `+`, `-`, `*`, `/`, `^` evaluation for typed numeric Enum initializer expressions, plus a `\`/`Mod` subset for operands with known Long coercion; integer division truncates toward zero and `Mod` uses the absolute-remainder rule. Integer-effective-width exceptions, zero divisors, and out-of-range operands remain unresolved; values near Single/Double rounding boundaries stay unresolved. Bounded floating/Currency arithmetic inside Const initializers is folded when the type and range are supported; Long-rounding boundaries, ambiguous Currency quantization, cycles, and unsupported values remain unresolved.
- Added `VBA2059` for Optional parameters with a statically resolved UDT type and `VBA2133` for supplied defaults proved to use a variable, procedure call, or `Is` operator instead of a constant expression. Reused existing LongLong, object, and array Let-coercion diagnostics for incompatible Optional defaults, and extended `VBA2131` to Null defaults used when an Optional argument is omitted. Unresolved names and external constants remain open-world.
- Propagated `ByRef` mutation candidates through nested in-project calls using resolved signatures, stopping at `ByVal`, `ParamArray`, ambiguous calls, and parenthesized value arguments.
- Added caller/callee path links from bounded `ByRef` write candidates to matching callee assignments, file reads, `ReDim`, `Erase`, loop counters, or nested-call writes; caller and callee conditions remain separate and unproven.
- Marks If/Select Case/While/Do paths and initial For bounds that traverse a provably false literal Boolean/small-integer predicate edge as `infeasible_constant_condition` and propagates that label through related caller/callee dataflow facts; all other structural paths remain `not_checked`.
- Added interprocedural call-input candidates for omitted Optional defaults, including explicit default expressions, declared-type initial values, and the Optional Variant Error 448 sentinel; links to callee reads remain bounded structural candidates, and default expressions are disclosure-gated in JSON.
- Added an opt-in word-stream p-code decoder driven entirely by a caller-supplied opcode/operand schema, with operation-type-specific operand layouts and generic fallbacks; no VBA opcode table is bundled and results remain unverified against an Office build.
- Added a decoder regression matrix for word, double-word, length-prefixed byte operands, unknown opcodes and operation types, truncation, instruction caps, and invalid caller schemas using synthetic data only.
- Restricted caller schema masks to contiguous, non-overlapping bit fields so opcode and operation-type extraction cannot silently discard gaps.
- Added exact operation-type schema definitions that select alternate operand layouts for a shared opcode, with explicit validation and generic fallbacks.
- Matched standalone `Stop` and `End` statements by exact unbracketed keyword tokens, preserving similarly prefixed names and leaving bracket expressions host-dependent.
- Matched `Resume` by its exact unbracketed keyword token so names such as `ResumeCount` do not become error-control statements.
- Kept square-bracket expressions out of VBA symbol resolution and added Excel-profile Evaluate access candidates, while preserving `!` dictionary keys.
- Distinguished `For Each` by its two keyword tokens so numeric loop counters beginning with `Each` stay ordinary `For` loops.
- Kept `Elsewhere` assignments, bracketed `Then`/`Else` expressions, and member names `In`/`To` out of block and loop clause delimiters.
- Excluded reserved member names after `.` / `!` from `Then` and `Else` detection in conditions and inline branches.
- Preprocessed continued `#If` / `#Const` directives as logical lines and carried duplicate-constant detection across physical continuation lines without shifting source offsets.
- Required line-continuation underscore to be preceded by whitespace and immediately precede the line ending, matching the specified line-continuation token.
- Added lexer/preprocessor support for full-width and Unicode Zs whitespace as VBA separators, including leading indentation before directives and declarations.
- Added a disclosure-aware JSON export for caller-schema p-code decode reports; decoded names, words, and payloads remain hidden unless source inclusion is requested.
- Added bounded interprocedural error-path candidates from resolved calls to callee unhandled-error sites, preserving separate caller/callee conditions and recording possible caller handler/`Resume Next`/default-policy outcomes.
- Applied the MS-VBAL event-handler error boundary: an unhandled error candidate in a `RaiseEvent` handler stops remaining event-handler dispatch and is not linked to recovery in the `RaiseEvent` caller.
- Seeded recognized host-entry error graphs with both direct-host `Terminate` and normal VBA-call `Default` contexts; affected CFGs remain incomplete because invocation origin is still a candidate.
- Diagnosed invalid implicit `LongLong` Let-coercion in assignments and known `ByVal` calls while leaving target-dependent `LongPtr` and general conversion rules unresolved.
- Recognized the `^` `LongLong` type character on names and integer literals while preserving caret exponentiation when followed by an operand.
- Replaced blanket binary numeric widening with operator-specific VBA result-type rules for known scalar operands, including Variant propagation, `LongLong`, dates, integer division, Mod, exponentiation, logical and relational operators.
- Exposed the raw MS-OVBA project-version tag in compiled-cache reports without treating it as an Office build identifier or cache compatibility proof.
- Inventoried MS-OVBA `__SRP_` performance-cache streams by spec-shaped name, length, and fingerprint while excluding them from module parsing and keeping contents opaque.
- Applied the MS-VBAL Enum-to-Long declared-type rule to expression, argument, interface-contract, and effective JSON types while preserving the source Enum name.
- Resolved nested UDT member reads and indexed UDT array fields for accessible in-project type declarations.
- Diagnosed UDT members without required `As` clauses, duplicate member names, and empty UDT bodies.
- Diagnosed UDT type-name collisions with enums, other visible UDTs, modules, projects, and referenced libraries within the scopes supported by the project metadata.
- Recognized fixed-length string type specifications, folded bounded integer arithmetic and accessible constant-reference chains with `Byte`/`Integer`/`Long` range checks, and warned when cycles, visibility, type coercions, or unsupported expressions leave a size unresolved.
- Diagnosed UDT members without required `As` clauses, duplicate members, and empty UDT bodies; bound leading-dot references to nested `With` expressions for type inference.
- Resolved typed method calls and direct argument, `ByRef`, and return-flow candidates for omitted-base calls inside nested `With` blocks.
- Resolved declared array indexing to element types, retained whole-array types, and diagnosed named subscripts or rank mismatches when rank is known.
- Extended path-feasibility classification to uniquely scoped procedure-local and same-module Boolean/integer `Const` values, including bounded integer Boolean/bitwise operations and VBA numeric `If` truthiness. Shadowed names, duplicate/cyclic constants, unsupported values, and paths whose feasibility cannot be proved remain unresolved.
- Added bounded string-constant, `+`/`&` concatenation, equality/order and `Like` folding for identical strings or ASCII strings/patterns under a valid `Option Compare Binary`; `compare_valid` records invalid or conditional-ambiguous module settings, while Text/Database collation and non-ASCII ordering/matching stay unresolved.
- Propagated literal and `Variant Const` Null through branch evaluation: relational/`Like` comparisons and `Not` with a known Null result remain Null, VBA `If` treats that condition as False, and bounded Boolean/Null `And`, `Or`, `Xor`, `Eqv`, and `Imp` cases use VBA truth tables. Other numeric/Null logical combinations stay unresolved.
- Added path-predicate folding and Boolean return typing for the built-in `IsNull`, `IsEmpty`, and `IsMissing` functions when their arguments are statically known and the intrinsic is not shadowed. `IsMissing` folds an Optional Variant formal for a Private standard-module procedure only when every resolved direct project call site agrees on whether the argument is omitted; mixed, public, dynamic, or unresolved call sources stay unknown. It also folds known non-missing ParamArray/non-Variant/defaulted cases; `VBA2148` diagnoses known wrong argument counts.
- Added Boolean type facts and path folding for `IsArray` when declarations establish a fixed/dynamic array or a known scalar; Variant and object payloads remain runtime-dependent.
- Added Boolean result typing and path folding for `IsObject` on declared Object/class variables, known scalar variables, and the `Nothing` literal; Variant and external/default-member state remains unresolved.
- Added a bounded `IsNumeric` path predicate: known numeric/Boolean value types and ASCII-digit-only strings are true, while Null/Empty are false; locale-formatted strings and unknown Variant payloads stay unresolved.
- Added `IsDate` Boolean result typing and wrong-arity diagnostics; folds Date literals, Date-typed expressions/calls, and selected unshadowed Variant(Date) intrinsics (`Date`, `Now`, `Time`, `DateAdd`, `DateSerial`, `DateValue`, `TimeSerial`, `CVDate`, and safe non-Variant `TimeValue` inputs). Locale-sensitive string recognition, nullable Variant results, and other coercions remain unresolved.
- Added `VarType` Integer result typing, wrong-arity diagnostics, and bounded subtype folding for static scalar literals/declarations, successful `CDec(...)` calls, declared arrays (`vbArray` plus element code), and `Array(...)` Variant arrays; dynamic Variant payloads, user-defined types, and object default-property results remain unresolved.
- Added `TypeName` String result typing, wrong-arity diagnostics, and static names for known built-in subtypes, supported arrays (with `()`), and Empty/Null values; dynamic Variant, object state, and object run-time types remain unresolved.
- Added `IsError` Boolean result typing and bounded path folding: known scalar subtypes are non-error, while successful unshadowed `CVErr(...)` values are errors; Variant payloads and object default-property results remain unresolved. Known wrong arities of `IsError` and `CVErr` use `VBA2148`.
- Propagated `CDec(...)`'s successful Variant/Decimal subtype into `VarType`, `TypeName`, and `IsNumeric` path predicates while retaining the Variant return type in general type inference.
- Feeds supported reaching assignments into branch predicates as assignment-time snapshots for path-specific Variant values and exact-type scalar assignments, including Null/Empty, literal subtypes, Array, CVErr, and CDec. Variant-to-Variant alias assignments snapshot only already-known values. Path facts are discarded across calls or unsupported writes and are not propagated through loops, member/array aliases, or incomplete CFGs.
- Resolves bare `Now` and `Time` intrinsic references under `Option Explicit` instead of reporting them as unresolved implicit Variant names.
- Applies intrinsic return types only to unshadowed bare-name calls, so a same-named in-project function or member is resolved by its declaration instead.
- Added path-specific argument snapshot facts for supported acyclic caller paths, preserving the value and caller conditions passed to a resolved callee parameter.
